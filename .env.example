# Local Postgres (docker compose)
DATABASE_URL=postgres://queue:queue@127.0.0.1:5434/queue

# Fetcher (optional override)
FETCH_MARKETS=ERCOT,MISO,PJM,CAISO,SPP,NYISO,ISO-NE

# PJM public queue export subscription key (from PJM interconnection web app)
PJM_QUEUE_SUBSCRIPTION_KEY=
