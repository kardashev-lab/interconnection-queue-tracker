# Web app

Next.js dashboard for the US Interconnection Queue. See the [project README](../README.md) for full setup.

```bash
cp .env.local.example .env.local
npm install
npm run dev
```

Requires Postgres with queue data. Run the fetcher from the repo root first.
